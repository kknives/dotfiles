- Clone fetching tree and blobs on demand, but get all commits

`git clone --filter=tree:0 <url>`

- Clone fetching blobs on demand, but get all commits and trees

`git clone --filter=blob:none <url>`
